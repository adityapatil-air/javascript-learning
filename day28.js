let index=0;

while (index<=10) {
    console.log(`value of index${index}`);
    index=index+2
}
let myArr=['don','rh','chamcha']
let arr=0
while (arr<myArr.length) {
    console.log(`hero is ${myArr[arr]}`);
    arr=arr+1
}

let age=0
do {
    console.log(`age is ${age}`);
    age++
} while (age<=18);